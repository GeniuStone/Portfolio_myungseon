let mongoose;_58e‍.x([["default",()=>_58e‍.o]]);_58e‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);/* 게시글의 스키마 */


// 스키마 객체 가져오고
const { Schema } = mongoose;

// 게시글의 스키마 작성
const PostSchema = new Schema({
    title: String,
    body: String,
    tags: [String],
    publishedDate: {
        type: Date,
        default: Date.now,
    },
    user : {
        _id : mongoose.Types.ObjectId,
        username : String
    }
});

// 게시글의 모델 생성 
// 모델은 스키마로의 인스턴스
const Post = mongoose.model('Post', PostSchema);
_58e‍.d(Post);
