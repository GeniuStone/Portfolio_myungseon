import Header from '../components/common/Header';

const SchedulePage = () => {
    return (
        <div>
            <Header></Header>
            <div>수업일정 페이지입니다.</div>
        </div>
    );
};

export default SchedulePage;
